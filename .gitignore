# Capturas de validación Playwright / Sphinx en raíz del repo
sphinx-*.png
*-validation-*.png
*.playwright-mcp-screenshot.png

.playwright-mcp/

# Build artifacts de Sphinx (se regeneran con `just docs-build`)
docs/_build/
docs/_sources/

# Variables de entorno reales. El .env de la raíz es la única fuente:
# se copia de .env.example (local) o .env.production.example (despliegue).
.env
.env.production
